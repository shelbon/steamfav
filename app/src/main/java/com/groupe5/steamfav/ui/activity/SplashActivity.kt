package com.groupe5.steamfav.ui.activity

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import com.groupe5.steamfav.R

class SplashActivity : AppCompatActivity() {
    /**
     * private lateinit var splashViewModel: SplashViewModel
    override fun oncreate(savedInstanceState: Bundle?){
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_splash)
        splashViewModel = ViewModelProviders.of(this.SplashFactory(
            this.))get(SplashViewModel::class.java)
        splashViewModel.progressBar()
        splashViewModel.handeler()
     **/
}